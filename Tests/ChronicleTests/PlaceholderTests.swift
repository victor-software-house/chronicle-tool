import Testing
import Foundation

/// Placeholder test so the ChronicleTests target compiles before the real
/// extraction tests land. Replace as Core/ modules are extracted.
@Test("placeholder smoke test")
func placeholder() {
  #expect(1 + 1 == 2)
}
